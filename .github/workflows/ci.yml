name: CI

on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  private-ready-gate:
    # This private review repo currently has no registered self-hosted runner, and
    # hosted Actions are billing-gated. Enable this only when a free runner lane
    # or intentional public-release Actions budget is available.
    if: vars.ENABLE_PRIVATE_REVIEW_CI == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Run private-ready publication smoke
        run: node scripts/final-review-smoke.mjs --expect-private-ready --skip-build
